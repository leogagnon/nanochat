horizon.target_param_data_ratio: -1
horizon.target_flops: 1e19
model.depth: 20

eval.core_metric_every: -1
eval.core_metric_max_per_task: -1
eval.sample_every: -1

optim.embedding_lr: 0.1
optim.unembedding_lr: 0.003
optim.matrix_lr: 0.005
optim.scalar_lr: 0.1

# 4 checkpoints every run
log.save_every: -1
log.num_checkpoints: 4

log.sweep_id: lower_test